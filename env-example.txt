# Google 웹 검색 API 설정 (Google Scholar가 아님!)
# Google Custom Search API는 일반 웹 검색을 위한 것입니다
# Get your API key from: https://console.cloud.google.com
GOOGLE_API_KEY=your-google-api-key-here

# Google Custom Search Engine ID
# 웹 검색을 위한 검색 엔진 ID (Google Scholar와 무관)
# Create one at: https://cse.google.com
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=your-custom-search-engine-id-here

# YouTube Data API v3
# Enable in Google Cloud Console and use the same or different API key
YOUTUBE_API_KEY=your-youtube-api-key-here

# ⚠️ 비용 경고:
# - Google Custom Search API: 100 쿼리/일 무료, 이후 $5/1000 쿼리
# - YouTube Data API: 10,000 유닛/일 무료 (검색당 100 유닛 소비)
# - Google Scholar: API 없음, scholarly 라이브러리 사용 (무료이나 차단 위험)

# Optional: Cache Configuration (defaults shown)
# CACHE_TTL=3600  # Cache time-to-live in seconds
# CACHE_MAX_SIZE=100  # Maximum cache entries per source

# Optional: Rate Limiting (defaults shown)
# RATE_LIMIT_DELAY=1.0  # Seconds between API requests
# SCHOLAR_RATE_LIMIT_DELAY=2.0  # Seconds between Google Scholar requests